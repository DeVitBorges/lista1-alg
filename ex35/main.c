#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("ex 35");

    float diag1, diag2;

    printf ("\ninforme o tamanho das duas diagonais, separadas por um espaco: ");
    scanf ("%f" "%f", &diag1, &diag2);
    printf ("\narea do losango: %.3f\n", (diag1*diag2)/2);

    return 0;
}
