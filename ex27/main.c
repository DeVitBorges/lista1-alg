#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("ex 27\n");

    float resultado, n1, n2;

    printf("insira os dois valores a serem divididos, os separando por um espaco: ");
    scanf("%f" "%f", &n1, &n2);
    resultado = (n1/n2);
    printf ("resultado da divisao: %.3f", resultado);

    return 0;
}
